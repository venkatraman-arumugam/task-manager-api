<source>
  @type forward
  port 24224
</source>

# Tag-based matching
<match redis>
  @type stdout
  @id redis-logs
</match>

<match flask>
  @type stdout
  @id flask-logs
</match>

<match celery>
  @type stdout
  @id celery-logs
</match>